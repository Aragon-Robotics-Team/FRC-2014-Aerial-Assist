#include "WIPLib.h"
#include "Gamepad.h"
//#include "Target.h" Is this used? -M
#include <SimpleRobot.h>
#include <AnalogChannel.h>
#include <math.h>

void main()
{
	/*Talon.spoolMotor;
	Talon.clutchMotor; not sure if ini goes here -K*/
	
	/*button initialization elsewhere*/
	
	/*spoolMotor = new Talon(x);
	clutchMotor = new Talon(y); */ //wat 
	
	bool isPressed = false;
	void winch()
	{
		while (whatever button is pressed || isPressed == true){
			spoolMotor(1);
			isPressed = true;
			if (someotherbutton == true) { //this locks the winch in position
					isPressed = false;
					clutchMotor(.05);
					spoolMotor(0);
					while (whatever button is depressed == true) { //this releases the lock on the winch, thus, releasing the winch
						clutchMotor(-.05);
						wait(2);
						clutchMotor(0);
					}
			}
		}
	}
}