/*basic winch stuff. not sure if it will be utilized in the final robot, b/c it is specific to a certain prototype.
still should proofread though*/

#include "WIPLib.h"
#include "Gamepad.h"
/*#include "Target.h"*/ //Is this used? -M
#include <SimpleRobot.h>
#include <AnalogChannel.h>
#include <math.h>

void main()
{
	/*Talon.spoolMotor;
	Talon.clutchMotor;*/
	
	/*button initialization elsewhere*/
	
	/*spoolMotor = new Talon(x);
	clutchMotor = new Talon(y); */ //wat 
	
	bool isPressed = false;
	void winch()
	{
		if (whatever button is depressed || isPressed == true){
			spoolMotor(1);
			isPressed = true;
			if (someotherbutton = true)
				{
					isPressed = false;
					clutchMotor(.05);
					while (whatever button is depressed == true) {
						clutchMotor(-.05);
						wait(2);
						clutchMotor(0);
					}
				}
		}
	}
}